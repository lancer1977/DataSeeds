name: DataSeeds
repo_root: /mnt/data/lancer1977/code/DataSeeds

languages:
  - csharp
  - markdown
  - yaml
frameworks:
  - dotnet
  - docs
  - docker

ci:
  build:
    - bash scripts/validate.sh
  test:
    - bash scripts/validate.sh

devstudio:
  runtime_model: three-agent
  backlog_ordering: filename
  skip_promoted_planning: true
  one_stage_per_run: true
